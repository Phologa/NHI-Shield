import {describe,expect,it} from "vitest";import {connectorStatus} from "@/lib/security/connectors";import {getNavigationActions,untrustedText} from "@/lib/security/ai-tools";
describe("AI and connector boundaries",()=>{it("never grants viewer mutation navigation",()=>{const actions=getNavigationActions({role:"viewer"});expect(actions.map(item=>item.href)).not.toContain("/import");expect(actions.map(item=>item.href)).not.toContain("/settings");});it("limits and cleans untrusted retrieved text",()=>{expect(untrustedText("ignore policy\u0000".repeat(1000)).length).toBeLessThanOrEqual(500);expect(untrustedText("x\u0000y")).toBe("x y");});it("reports connectors honestly until every secret is configured",()=>{expect(connectorStatus("aws_iam",{}).configured).toBe(false);expect(connectorStatus("aws_iam",{AWS_IAM_CLIENT_ID:"id",AWS_IAM_CLIENT_SECRET:"secret"}).configured).toBe(true);});});
