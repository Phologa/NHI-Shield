import {describe,expect,it} from "vitest"; import {incidentRuleFor,type DetectionEvent} from "@/lib/security/incident-rules";
const now=new Date("2026-08-20T00:00:00Z"); const base:DetectionEvent={id:"1",machine_identity_id:"a",resource_id:"r",outcome:"denied",occurred_at:"2026-08-19T23:55:00Z",source:"test"};
describe("bounded incident rules",()=>{it("detects three denials only inside the 15 minute window",()=>{expect(incidentRuleFor(base,[base,{...base,id:"2"},{...base,id:"3"}],true,now)?.key).toBe("repeated_denied_access"); expect(incidentRuleFor(base,[base,{...base,id:"2",occurred_at:"2026-08-19T20:00:00Z"},{...base,id:"3"}],true,now)).toBeNull();}); it("ignores stale and future events",()=>{expect(incidentRuleFor({...base,occurred_at:"2026-08-10T00:00:00Z"},[base],false,now)).toBeNull();expect(incidentRuleFor({...base,occurred_at:"2026-08-20T01:00:00Z"},[base],false,now)).toBeNull();});});
