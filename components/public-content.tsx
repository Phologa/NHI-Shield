export function PageIntro({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }) { return <section className="border-b border-[var(--line)] bg-white"><div className="mx-auto max-w-7xl px-6 py-20 lg:px-10 lg:py-28"><p className="sans text-sm font-semibold uppercase tracking-[0.2em] text-[var(--teal)]">{eyebrow}</p><h1 className="mt-5 max-w-4xl font-serif text-5xl leading-[1.02] md:text-6xl">{title}</h1><p className="sans mt-7 max-w-3xl text-lg leading-8 text-[var(--muted)]">{description}</p></div></section>; }

export function ContentSection({ title, children, tone = "paper" }: { title: string; children: React.ReactNode; tone?: "paper" | "white" | "teal" }) { const background = tone === "white" ? "bg-white" : tone === "teal" ? "bg-[#e4efee]" : "bg-[var(--paper)]"; return <section className={`border-b border-[var(--line)] ${background}`}><div className="mx-auto max-w-7xl px-6 py-20 lg:px-10"><h2 className="max-w-3xl font-serif text-3xl leading-tight md:text-4xl">{title}</h2><div className="sans mt-8 max-w-4xl text-[var(--muted)] [&_li]:mb-3 [&_p]:mb-5 [&_p]:leading-7">{children}</div></div></section>; }

export function StatusTag({ children }: { children: React.ReactNode }) { return <span className="sans text-xs font-semibold uppercase tracking-wider text-[var(--teal)]">{children}</span>; }