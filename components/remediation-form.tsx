"use client";
import { useActionState } from "react";
import { proposeRemediation, type RemediationResult } from "@/lib/security/remediation-actions";
const initial: RemediationResult = { ok: true };
export function RemediationForm() { const [state, action, pending] = useActionState(proposeRemediation, initial); return <form action={action} className="border border-[var(--line)] bg-white p-6"><div className="grid gap-4"><label className="font-sans text-sm font-medium">Recommended action<input className="focus-ring mt-2 w-full border border-[var(--line)] p-3" name="title" placeholder="Rotate the expired credential" required /></label><label className="font-sans text-sm font-medium">Action type<input className="focus-ring mt-2 w-full border border-[var(--line)] p-3" name="actionType" placeholder="credential_rotation" required /></label><label className="font-sans text-sm font-medium">Why this is recommended<textarea className="focus-ring mt-2 min-h-28 w-full border border-[var(--line)] p-3" name="rationale" required /></label></div>{state.error && <p className="mt-3 text-sm text-red-700">{state.error}</p>}{state.message && <p className="mt-3 bg-[var(--teal-soft)] p-3 text-sm">{state.message}</p>}<button className="focus-ring mt-4 bg-[var(--navy)] px-5 py-3 font-sans text-sm font-semibold text-white" disabled={pending}>{pending ? "Saving…" : "Propose action"}</button></form>; }
