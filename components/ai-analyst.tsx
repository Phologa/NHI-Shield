"use client";
import Link from "next/link";
import { useActionState, useEffect, useRef, useState } from "react";
import { askSecurityAnalyst, type AiResult } from "@/lib/security/ai-actions";
const initial: AiResult = { ok:true, messages:[] };
const suggestions=["Which identities have the highest risk?","Explain this finding.","What should I investigate first?"];
export function AiAnalyst({pageContext="AI Analyst"}:{pageContext?:string}) { const [state,action,pending]=useActionState(askSecurityAnalyst,initial); const [question,setQuestion]=useState(""); const end=useRef<HTMLDivElement>(null); useEffect(()=>{ end.current?.scrollIntoView({behavior:"smooth"}); },[state.messages?.length,pending]); return <div className="overflow-hidden rounded-xl border border-[var(--line)] bg-white"><div className="max-h-[34rem] space-y-4 overflow-y-auto p-4 sm:p-6" aria-live="polite">{!state.messages?.length&&<div className="flex flex-wrap gap-2">{suggestions.map((text)=><button className="focus-ring rounded-full border border-[var(--line)] px-3 py-2 text-left text-xs font-semibold text-[var(--navy)] hover:bg-[#eef4f7]" key={text} onClick={()=>setQuestion(text)} type="button">{text}</button>)}</div>}{state.messages?.map((message,index)=><article className={`max-w-3xl rounded-lg p-4 text-sm leading-6 ${message.role==="user"?"ml-auto bg-[var(--navy)] text-white":"bg-[#f4f8f9]"}`} key={`${message.role}-${index}`}><p className="mb-2 text-xs font-bold uppercase tracking-wider opacity-70">{message.role==="user"?"You":"NHI Shield AI"}</p><div className="whitespace-pre-wrap">{message.content}</div>{message.citations?.length?<div className="mt-4 border-t border-[var(--line)] pt-3"><p className="text-xs font-semibold">Relevant records</p><div className="mt-2 flex flex-wrap gap-2">{message.citations.map((citation)=><Link className="focus-ring border border-[var(--line)] bg-white px-3 py-2 text-xs font-semibold text-[var(--teal)]" href={citation.href} key={citation.href}>{citation.label}</Link>)}</div></div>:null}{message.actions?.length?<div className="mt-3 flex flex-wrap gap-2">{message.actions.map((item)=><Link className="focus-ring bg-[var(--teal)] px-3 py-2 text-xs font-semibold text-white" href={item.href} key={item.href}>{item.label}</Link>)}</div>:null}</article>)}{pending&&<p className="text-sm text-[var(--muted)]" role="status">Analysing authorised records…</p>}{state.error&&<p className="border-l-4 border-amber-600 bg-amber-50 p-4 text-sm text-amber-900" role="alert">{state.error}</p>}<div ref={end}/></div><form action={action} className="border-t border-[var(--line)] p-4 sm:p-6"><input name="conversationId" type="hidden" value={state.conversationId??""}/><input name="pageContext" type="hidden" value={pageContext}/><label className="sr-only" htmlFor="question">Ask NHI Shield AI</label><textarea className="focus-ring min-h-20 w-full rounded-lg border border-[var(--line)] p-3 text-sm" id="question" maxLength={2000} name="question" onChange={(event)=>setQuestion(event.target.value)} placeholder="Ask about identity and access risk…" required value={question}/><div className="mt-3 flex justify-end"><button className="focus-ring rounded-lg bg-[var(--teal)] px-5 py-3 text-sm font-semibold text-white disabled:opacity-60" disabled={pending} onClick={()=>setTimeout(()=>setQuestion(""),0)}>{pending?"Analysing…":"Send"}</button></div></form></div>; }
