import { ContentSection, PageIntro, StatusTag } from "@/components/public-content";

const principles = ["Defence in depth", "Least privilege", "Tenant isolation", "Server-side authorization", "PostgreSQL Row Level Security", "Secure credential handling", "Auditability", "Evidence-backed decisions", "Human approval for high-impact remediation", "Separated public and protected surfaces"];
export default function SecurityPage() { return <><PageIntro eyebrow="Security principles" title="Trust is designed into the foundation." description="NHI Shield communicates security through architecture and accountable controls, not exaggerated certification claims." /><ContentSection title="Implemented foundation" tone="white"><div className="grid gap-4 sm:grid-cols-2">{principles.map((principle) => <div className="border border-[var(--line)] p-5 text-sm font-semibold text-[var(--ink)]" key={principle}>{principle}</div>)}</div><p className="mt-8">The current foundation includes Supabase Auth session handling, server-side permission checks, organisation membership boundaries, PostgreSQL Row Level Security and a clear separation between public product pages and protected application routes.</p></ContentSection><ContentSection title="Planned advanced security capability"><StatusTag>Under development</StatusTag><p className="mt-5">Machine identity discovery, access analysis, evidence-backed findings, AI-assisted investigation and controlled remediation belong to later milestones. They are not represented as available functionality here.</p></ContentSection><ContentSection title="What we do not claim"><p>NHI Shield does not currently claim ISO 27001, SOC 2, POPIA, penetration-test or government security accreditation.</p></ContentSection></>; }