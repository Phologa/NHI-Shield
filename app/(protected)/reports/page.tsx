import Link from "next/link";
import {requireSecurityContext} from "@/lib/security/context";
import {getOverviewCounts} from "@/lib/security/repositories";
export default async function ReportsPage(){
 const context=await requireSecurityContext("view_security_data");const counts=await getOverviewCounts(context);
 return <section><p className="text-sm font-semibold uppercase tracking-wider text-[var(--teal)]">Understand and share your position</p><div className="mt-3 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 className="font-serif text-4xl">Security report</h2><p className="mt-3 text-sm text-[var(--muted)]">Current facts and deterministic results for {context.organisationName}.</p></div><Link className="focus-ring bg-[var(--teal)] px-5 py-3 text-center text-sm font-semibold text-white" href="/api/reports/security-summary">Download detailed CSV</Link></div><div className="mt-8 border border-[var(--line)] bg-white p-5 sm:p-8"><h3 className="font-serif text-2xl">Executive summary</h3><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{[["Apps & system accounts",counts.identities],["Active risks",counts.activeFindings],["High or critical risks",counts.criticalHighFindings],["Active incidents",counts.activeIncidents],["Accounts needing an owner",counts.identitiesWithoutOwners],["Recent activity records",counts.activityEvents]].map(([label,value])=><div className="bg-[#eef4f7] p-5" key={label}><p className="text-xs uppercase tracking-wider text-[var(--muted)]">{label}</p><p className="mt-3 font-serif text-3xl">{value}</p></div>)}</div><p className="mt-6 text-xs leading-5 text-[var(--muted)]">The download includes risks, incidents, recommended actions and audit history. Spreadsheet formula characters are neutralised. No AI-generated probability is included.</p></div></section>;
}
