import Link from "next/link";
import { AnalysisButton } from "@/components/security-forms";
import { requireSecurityContext } from "@/lib/security/context";
import { getOverviewCounts } from "@/lib/security/repositories";
import { hasPermission } from "@/lib/security/permissions";

export default async function OverviewPage() {
  const context = await requireSecurityContext("view_security_data"); const counts = await getOverviewCounts(context); const canManage = hasPermission(context.role, "manage_security_inventory"); const hasData = counts.identities + counts.resources > 0; const urgent = counts.criticalHighFindings + counts.highCriticalIncidents;
  const next = !hasData ? { eyebrow: "Build visibility", title: "Add your company data", body: "Import apps, system accounts, resources and their access. You will review every row before anything is saved.", href: "/import", label: "Import company data" } : urgent > 0 ? { eyebrow: "Priority response", title: `Review ${urgent} urgent item${urgent === 1 ? "" : "s"}`, body: "Start with high and critical findings and incidents. Each record includes the evidence behind it and a recommended next step.", href: "/findings", label: "Review urgent risks" } : counts.identitiesWithoutOwners > 0 ? { eyebrow: "Improve accountability", title: `Assign ${counts.identitiesWithoutOwners} missing owner${counts.identitiesWithoutOwners === 1 ? "" : "s"}`, body: "Unowned machine identities are harder to review, rotate and retire safely. Confirm who is accountable for each one.", href: "/machine-identities", label: "Review identity inventory" } : { eyebrow: "Continue monitoring", title: "Review current access paths", body: "See which apps and system accounts can reach sensitive resources, including observed privileged access.", href: "/access-graph", label: "Explore relationships" };
  const status = urgent > 0 ? { label: "Attention required", colour: "bg-red-500", text: `${urgent} high-priority item${urgent === 1 ? "" : "s"} need review` } : !hasData ? { label: "Visibility incomplete", colour: "bg-amber-500", text: "Import inventory data to establish your security posture" } : { label: "Monitoring", colour: "bg-emerald-500", text: "No high-priority open items in the current data" };
  const metricCards = [
    { label: "Urgent risks", value: counts.criticalHighFindings, help: "High and critical open findings", href: "/findings", tone: counts.criticalHighFindings ? "text-red-700" : "text-[var(--navy)]" },
    { label: "Active incidents", value: counts.activeIncidents, help: "Events under investigation", href: "/incidents", tone: counts.highCriticalIncidents ? "text-red-700" : "text-[var(--navy)]" },
    { label: "Machine identities", value: counts.identities, help: `${counts.identitiesWithoutOwners} without a confirmed owner`, href: "/machine-identities", tone: counts.identitiesWithoutOwners ? "text-amber-700" : "text-[var(--navy)]" },
    { label: "Access paths", value: counts.relationships, help: `${counts.privilegedRelationships} marked privileged`, href: "/access-graph", tone: counts.privilegedRelationships ? "text-amber-700" : "text-[var(--navy)]" },
  ];
  return <section><div className="flex flex-col gap-5 border-b border-[var(--line)] pb-7 sm:flex-row sm:items-end sm:justify-between"><div><div className="flex items-center gap-2"><span className={`h-2.5 w-2.5 rounded-full ${status.colour}`} /><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--muted)]">{status.label}</p></div><h2 className="mt-3 text-3xl font-semibold tracking-tight text-[var(--navy)] sm:text-4xl">Security operations overview</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-[var(--muted)]">{status.text}. This view reflects only tenant-scoped data recorded for {context.organisationName}.</p></div>{canManage && <AnalysisButton />}</div>
    <div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metricCards.map((card) => <Link className="panel focus-ring group p-5 transition hover:-translate-y-0.5 hover:border-[var(--teal)] hover:shadow-md" href={card.href} key={card.label}><div className="flex items-start justify-between"><p className="text-xs font-bold uppercase tracking-[.12em] text-[var(--muted)]">{card.label}</p><span className="text-sm text-slate-300 group-hover:text-[var(--teal)]">↗</span></div><p className={`mt-4 text-4xl font-semibold tracking-tight ${card.tone}`}>{card.value}</p><p className="mt-2 text-xs leading-5 text-[var(--muted)]">{card.help}</p></Link>)}</div>
    <div className="mt-6 grid gap-6 xl:grid-cols-[1.6fr_1fr]"><article className="overflow-hidden rounded-xl bg-[var(--navy)] text-white shadow-sm"><div className="border-b border-white/10 px-6 py-4"><p className="text-[10px] font-bold uppercase tracking-[.2em] text-[var(--cyan)]">What should I do next?</p></div><div className="p-6 sm:p-8"><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">{next.eyebrow}</p><h3 className="mt-3 text-2xl font-semibold sm:text-3xl">{next.title}</h3><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">{next.body}</p><Link className="focus-ring mt-6 inline-flex rounded-lg bg-[var(--cyan)] px-4 py-3 text-sm font-bold text-[var(--navy)]" href={next.href}>{next.label}<span className="ml-2">→</span></Link></div></article>
      <article className="panel p-6"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[var(--teal)]">Coverage</p><h3 className="mt-2 text-lg font-semibold text-[var(--navy)]">What NHI Shield can see</h3></div><span className="rounded-lg bg-[var(--teal-soft)] px-2 py-1 text-[10px] font-bold uppercase text-[var(--teal)]">Live data</span></div><dl className="mt-5 divide-y divide-[var(--line)] text-sm">{[["Protected resources", counts.resources], ["Credential records", counts.credentials], ["Active findings", counts.activeFindings], ["Security activity records", counts.activityEvents]].map(([label, value]) => <div className="flex items-center justify-between py-3" key={label}><dt className="text-[var(--muted)]">{label}</dt><dd className="font-semibold text-[var(--navy)]">{value}</dd></div>)}</dl><Link className="focus-ring mt-4 inline-block text-sm font-semibold text-[var(--teal)]" href="/data-sources">View data sources →</Link></article></div>
    <div className="mt-6 grid gap-6 lg:grid-cols-[1.6fr_1fr]"><article className="panel p-6"><div className="flex items-end justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[var(--muted)]">Operational workflow</p><h3 className="mt-2 text-lg font-semibold text-[var(--navy)]">From discovery to action</h3></div></div><ol className="mt-5 grid gap-3 sm:grid-cols-3"><li className="rounded-lg border border-[var(--line)] bg-slate-50 p-4"><span className="text-xs font-bold text-[var(--teal)]">01 · DISCOVER</span><p className="mt-2 text-sm font-semibold">Inventory identities and access</p><p className="mt-1 text-xs leading-5 text-[var(--muted)]">Import observed data and identify ownership gaps.</p></li><li className="rounded-lg border border-[var(--line)] bg-slate-50 p-4"><span className="text-xs font-bold text-[var(--teal)]">02 · UNDERSTAND</span><p className="mt-2 text-sm font-semibold">Assess evidence-backed risk</p><p className="mt-1 text-xs leading-5 text-[var(--muted)]">Review findings, incidents and access context.</p></li><li className="rounded-lg border border-[var(--line)] bg-slate-50 p-4"><span className="text-xs font-bold text-[var(--teal)]">03 · RESPOND</span><p className="mt-2 text-sm font-semibold">Approve and audit action</p><p className="mt-1 text-xs leading-5 text-[var(--muted)]">Use guided remediation with a clear record.</p></li></ol></article><Link className="panel focus-ring group p-6 hover:border-[var(--teal)]" href="/ai-analyst"><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-[var(--teal-soft)] text-xs font-bold text-[var(--teal)]">AI</div><h3 className="mt-4 text-lg font-semibold text-[var(--navy)]">Ask NHI Shield AI</h3><p className="mt-2 text-sm leading-6 text-[var(--muted)]">Ask plain-English questions about your current identities, access, risks and incidents.</p><p className="mt-4 text-sm font-semibold text-[var(--teal)]">Open AI Analyst →</p></Link></div>
  </section>;
}
