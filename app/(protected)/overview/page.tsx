import Link from "next/link"; import {AnalysisButton} from "@/components/security-forms"; import {requireSecurityContext} from "@/lib/security/context"; import {getOverviewCounts} from "@/lib/security/repositories"; import {hasPermission} from "@/lib/security/permissions";
export default async function OverviewPage(){const context=await requireSecurityContext("view_security_data");const counts=await getOverviewCounts(context);const canManage=hasPermission(context.role,"manage_security_inventory");const hasData=counts.identities+counts.resources>0;const danger=counts.criticalHighFindings+counts.highCriticalIncidents;const next=!hasData?{title:"Add your company data",body:"Upload a spreadsheet of apps and system accounts. You can review every row before anything is saved.",href:"/import",label:"Add company data"}:danger>0?{title:`Review ${danger} urgent item${danger===1?"":"s"}`,body:"Start with high and critical risks. NHI Shield shows the recorded evidence and recommended next step.",href:"/findings",label:"Review urgent risks"}:counts.activeIncidents>0?{title:"Investigate active security activity",body:"Open incidents to see what happened, when it happened and which account was involved.",href:"/incidents",label:"Open incidents"}:{title:"Ask what to do next",body:"The AI Analyst can explain your current security position in simple English.",href:"/ai-analyst",label:"Ask NHI Shield AI"};return <section><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold uppercase tracking-wider text-[var(--teal)]">Your security control centre</p><h2 className="mt-3 font-serif text-4xl">Good {new Date().getHours()<12?"morning":"day"}, here’s what matters</h2><p className="mt-4 text-sm text-[var(--muted)]">{context.organisationName} · {context.role==="viewer"?"Read-only view":"Security workspace"}</p></div>{canManage&&<AnalysisButton/>}</div><div className="mt-8 grid gap-4 lg:grid-cols-3"><div className="border-l-4 border-[var(--teal)] bg-white p-6 lg:col-span-2"><p className="text-xs font-bold uppercase tracking-wider text-[var(--teal)]">What should I do next?</p><h3 className="mt-3 font-serif text-2xl">{next.title}</h3><p className="mt-3 max-w-2xl text-sm leading-6 text-[var(--muted)]">{next.body}</p><Link className="focus-ring mt-5 inline-block bg-[var(--teal)] px-5 py-3 text-sm font-semibold text-white" href={next.href}>{next.label}</Link></div><Link className="focus-ring bg-[var(--navy)] p-6 text-white" href="/ai-analyst"><p className="text-xs font-bold uppercase tracking-wider text-[var(--cyan)]">Need help understanding this?</p><h3 className="mt-3 font-serif text-2xl">Speak to our AI</h3><p className="mt-3 text-sm leading-6 text-slate-300">Ask questions in plain English about risks, incidents, access or how to use NHI Shield.</p></Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Urgent risks",counts.criticalHighFindings,"/findings","Security problems to review first"],["Active incidents",counts.activeIncidents,"/incidents","Activity that may need investigation"],["Apps & system accounts",counts.identities,"/machine-identities","Non-human accounts being protected"],["Accounts without owners",counts.identitiesWithoutOwners,"/machine-identities","Accounts that need a responsible person"]].map(([label,value,href,help])=><Link className="focus-ring border border-[var(--line)] bg-white p-5 hover:border-[var(--teal)]" href={href as string} key={label as string}><p className="text-sm font-semibold">{label}</p><p className="mt-3 font-serif text-4xl">{value}</p><p className="mt-2 text-xs leading-5 text-[var(--muted)]">{help}</p></Link>)}</div><div className="mt-8"><h3 className="font-serif text-xl">Your simple security journey</h3><ol className="mt-4 grid gap-2 text-sm sm:grid-cols-2 lg:grid-cols-7">{["Add company data","Review risks","Investigate","Ask AI","Review action","Approve","Audit"].map((step,index)=><li className="border border-[var(--line)] bg-[#eef4f7] p-3" key={step}><span className="font-bold text-[var(--teal)]">{index+1}.</span> {step}</li>)}</ol></div></section>}
