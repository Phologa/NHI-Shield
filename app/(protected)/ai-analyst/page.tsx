import { AiAnalyst } from "@/components/ai-analyst";
import { requireSecurityContext } from "@/lib/security/context";
export default async function AiAnalystPage() { await requireSecurityContext("view_security_data"); return <section><p className="text-sm font-semibold uppercase tracking-wider text-[var(--teal)]">Security help in simple English</p><h2 className="mt-3 font-serif text-4xl">Ask NHI Shield AI</h2><p className="mt-4 max-w-3xl text-sm leading-6 text-[var(--muted)]">Ask follow-up questions about your company’s risks, incidents, accounts, access and evidence—or ask where to go next. NHI Shield uses controlled, read-only tools and never gives the AI direct database access.</p><div className="mt-8"><AiAnalyst /></div></section>; }
