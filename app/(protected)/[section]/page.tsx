type Props = { params: Promise<{ section: string }> };
const labels: Record<string, string> = { "machine-identities": "Machine Identities", "access-graph": "Access Graph", findings: "Findings", incidents: "Incidents", "ai-security-analyst": "AI Security Analyst", remediation: "Remediation", "audit-log": "Audit Log", settings: "Settings" };
export default async function SectionPage({ params }: Props) { const { section } = await params; const title = labels[section] ?? "Workspace"; return <section><p className="sans text-sm font-semibold uppercase tracking-wider text-[var(--teal)]">Not yet configured</p><h2 className="mt-3 font-serif text-4xl">{title}</h2><div className="mt-10 border border-[var(--line)] bg-white p-8"><p className="sans text-sm leading-6 text-[var(--muted)]">This navigation foundation is reserved for a future milestone. No security data or simulated results are displayed.</p></div></section>; }